export enum AccountType {
  REGULATOR = 0,
  FARMER = 1,
  MANUFACTURER = 2,
  RETAILER = 3,
  CONSUMER = 4,
  LOGISTICS = 5,
  ORACLE = 6,
}

export enum AccountStatus {
  PENDING = 0,
  APPROVED = 1,
  REJECT = 2,
}
